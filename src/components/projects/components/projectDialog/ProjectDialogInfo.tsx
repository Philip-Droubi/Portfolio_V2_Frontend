import type { ProjectType } from "../../types"

export default function ProjectDialogInfo({
    project,
    lang,
}: {
    project: ProjectType,
    lang: string,
}) {
    return (
        <div>ProjectDialogInfo</div>
    )
}
